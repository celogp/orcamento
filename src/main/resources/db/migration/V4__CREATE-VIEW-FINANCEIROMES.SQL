CREATE VIEW VFINANCEIROMES ( ANO, MES, RECEITA, VALOR) AS
SELECT
MONTH(f.DTMOVIMENTO) AS ANO,
MONTH(f.DTMOVIMENTO) AS MES,
f.receita as RECEITA,
SUM(f.VLRFINANCEIRO) as VALOR
FROM TFINANCEIROS f
GROUP BY MONTH(f.DTMOVIMENTO), MONTH(f.DTMOVIMENTO), f.RECEITA